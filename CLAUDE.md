# Project context

This project keeps its agent instructions in `AGENTS.md` so that every coding
agent — not just Claude — reads the same guidance. The deep references live in
`docs/camera-kit-web.md` (Camera Kit Web SDK) and `docs/audio-capture.md` (the
custom recording / audio-capture mechanism).

@AGENTS.md
